if __name__ == '__main__':
    # Add function
    # add = lambda x, y: x + y
    print((lambda x, y: x + y)(2, 3))

    # Other examples
    # lambda x: x % 2 == 0
    # lambda x, y: x if x > y else y
    # lambda dict: dict['name']
    # lambda list: all([len(e) >= 3 for e in list])